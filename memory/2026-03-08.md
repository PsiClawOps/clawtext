---
date: 2026-03-08
project: rgcs
type: decision
entities: RageFX,FidelityFX,FSR,LUT,SteamVR,compositor
keywords: LUT,DaVinci,FidelityFX,FSR,sharpening,phase1,phase2,FOV
---

## RageFX: prioritize LUT support and FSR/sharpening pipeline (phase plan)

Decision: RageFX will prioritize adding LUT capability (user uses DaVinci 65 LUTs) and adopt a FidelityFX/FSR-based sharpening/resolution pipeline for phase 1. Phase 2 will explore faking a wider FOV by shimming the compositor before the compositor sees the headset (a distortion/FOV shim). Actionable: implement LUT import support, wire RCAS/FSR sharpening first, then prototype compositor-level FOV shim in a later phase.


---

date: 2026-03-08
project: infrastructure
type: decision
entities: VSCode,WiX,MSI,git,versioning,qml
keywords: packaging,VSCode,WiX,MSI,versioning,qml-cache,build-pipeline,validation
---

## Packaging and build policy for RageFX: VSCode + private git + WiX MSI + deterministic versioning

Decision: Package the compositor (compositor-fx / RageFX) as a private git repo for VSCode development, produce an MSI via WiX for distribution, and enforce strict versioning: start at 1.0.0 and increment automatically on atomic events (e.g., commits). Ensure a hygienic install (clear QML cache), full clean builds, validation checks in the pipeline, and compatibility with the existing RGCS build MCP server. Actionable: add to_vscode.md with build and install instructions, copy WiX installer metadata from RGCS, and include version info in startup/log headers.


---

date: 2026-03-08
project: rgcs
type: fact
entities: compositor-fx,repo,workspace,overlay,steamvr
keywords: repo-scaffold,compositor-fx,overlay,steamvr,logs,git,VSCode
---

## Compositor-fx scaffold ready and next steps for repo/overlay

Fact: The compositor-fx package is scaffolded at `~/.openclaw/workspace/compositor-fx/` and the user requested it be packaged into a new private git repo and connected to VSCode. They also flagged that an overlay/SteamVR dashboard app (like RGCS) is missing and critical; the overlay must register in SteamVR and produce logs on startup. Actionable: create repo, push scaffold, add `to_vscode.md`, and ensure overlay manifest + registration mirror RGCS.


---

date: 2026-03-08
project: openclaw
type: fact
entities: openclaw,agent,openclaw.json,openai-codex,capabilities
keywords: onboarding,capabilities=none,agent-sandbox,sandboxing,openclaw.json,restore,backup
---

## OpenClaw onboarding can remove tools (capabilities=none) — observed state and remediation

Fact: After running the OpenClaw onboarding for OpenAI (openai-codex/gpt-5.1), the running agent was observed with `capabilities=none` (no web/file/shell tools). This indicates the agent definition was replaced with a minimal, tool-less config. Recommended remediation: restore `openclaw.json` from a pre-onboard backup (if present) or patch the agent definition to re-enable desired tools (web_search, exec, file) while keeping the new model wiring, then restart the gateway. Actionable: check `~/.openclaw/openclaw.json` for backups and agent block and either restore or apply a minimal patch.


---

date: 2026-03-08
project: clawtext
type: fact
entities: mindgardener,ClawText,garden.js,staging,memory
keywords: mindgardener,garden,garden.js,consolidation,ClawText,validation,reminder
---

## Mindgardener reminder: run garden script to consolidate .learnings into memory/staging and validate with ClawText

Fact: A scheduled reminder asked to run `node ~/.openclaw/workspace/skills/mindgardener/garden.js` to consolidate `.learnings` into memory/staging and validate them with ClawText. Actionable: run the script regularly (or now) to feed validated learnings into the memory pipeline and ClawText clusters.


---

date: 2026-03-08
project: general
type: fact
entities: together.ai,image-generation,ragefx_icon,overlay,assets
keywords: together.ai,image,prompt,ragefx,icon,overlay,assets
---

## RageFX icon generation prompt and asset path

Fact: An image generation prompt for a retro graphics-card style "RAGEFX" icon was prepared for together.ai. The intended save path is `overlay/assets/ragefx_icon.png`. Actionable: attempt generation using the provided prompt and save the result at the stated path; if generation fails, capture the service error for debugging.


---

date: 2026-03-08
project: infrastructure
type: fact
entities: qwen,qwen3_embedding,embeddings,config
keywords: embeddings,qwen/qwen3-embedding-8b,config,model,vector-store,search
---

## Embedding model configured: qwen/qwen3-embedding-8b

Fact: The embedding configuration reported in-session is `"model": "qwen/qwen3-embedding-8b"`. Actionable: verify embeddings pipeline (indexing -> search) uses this model and confirm the gateway/restart steps if necessary.


---

date: 2026-03-08
project: embeddings
type: decision
entities: OpenRouter,qwen3-embedding-8b,qwen3-embedding-4b,embedding-models
keywords: embeddings,OpenRouter,cost,quality,benchmark,qwen3-8b,qwen3-4b
---

## Embedding model migration: qwen3-embedding-8b selected over 4b (lower cost, higher dimensions)

Decision: Switched embedding model from local Ollama (nomic-embed-text, 768 dims, high CPU) to OpenRouter `qwen/qwen3-embedding-8b` (4096 dims, MRL-capable). Selected 8b over 4b because:
- 8b is cheaper on OpenRouter (better rate)
- 4096 dimensions > 2560 dimensions (higher quality)
- Memory search tested functional (3 results returned for "RGCS" query, scores 0.61-0.63)
- 8b ranks #1 on MTEB multilingual leaderboard (70.58)

Config location: `/home/lumadmin/.openclaw/openclaw.json` (memorySearch.model). Requires `openclaw gateway restart` to activate. Actionable: restart gateway and validate memory search continues working as expected.


---

date: 2026-03-08
project: openclaw
type: investigation
entities: openclaw.json,onboard,skills-restriction,agents,clawsaver
keywords: onboard,agent-config,skills,clawsaver-restriction,timeline,backup-analysis
---

## OpenClaw agent config: Timeline of skills restriction introduction

**Timeline Analysis (via openclaw.json backups):**

Feb 22-23: agents.list had 3 agents (main, background-light, background-ml), NO skills restriction, model: kimi-k2.5
Mar 5 07:01: Backup marker (content truncated, unclear state)
Mar 7 05:33 onwards: agents.list[0] changed to "channel-mini" WITH "skills": ["clawsaver"] restriction, model: github-copilot/gpt-5-mini

**Root cause:** Sometime between Feb 23 and Mar 7 (likely during an `openclaw onboard` command or manual config edit), the agent config was rewritten to:
1. Replace all 3 agents with a single "channel-mini" agent
2. Add "skills": ["clawsaver"] that restricts the agent to only the clawsaver skill
3. This caused capabilities=none because clawsaver doesn't provide web/file/shell tools

**What "skills" restriction does:**
- When present, "skills" acts as a whitelist — ONLY those skills are loaded
- When absent, all default skills/capabilities are available
- clawsaver is just message batching; it provides no tools

**Fix applied (2026-03-08 04:08 UTC):**
Removed "skills": ["clawsaver"] line from agents.list[0], allowing default capabilities to load. Verified: web_search now works.

**Hypothesis:** `openclaw onboard` (used to add openai-codex subscription) may have reset the agent config to a minimal template that included this skills restriction. The backup from Mar 5 might show the intermediate state.

**Action:** Keep agents.list[0] without skills restriction unless there's a specific reason to limit it. If clawsaver is needed for batching, keep it but add back the full tool set: skills: ["clawsaver", ...default skills...]

---

date: 2026-03-08
project: openclaw
type: fact
entities: channel-mini,skills-restriction,fix,gateway,capabilities
keywords: capabilities,fix-confirmed,web_search,memory-search,tools,session-recovery
---

## Post-fix session state: tools confirmed working (04:11 UTC)

Fact: After removing the `"skills": ["clawsaver"]` restriction from the `channel-mini` agent config and restarting the gateway, tools are confirmed functional:
- `web_search` working ✅
- `memory_search` working ✅ (returned results for "RGCS" query)
- Embedding model: `qwen/qwen3-embedding-8b` via OpenRouter (4096 dims, MRL-capable)
- Session model: `github-copilot/claude-sonnet-4.6` (switched from gpt-5-mini mid-session)

Session recovering from compaction at 04:11 UTC. Pre-compaction flush performed; all key decisions and facts for 2026-03-08 captured in this file. No data loss expected.

Pending items at compaction:
- compositor-fx repo: scaffold exists at `~/.openclaw/workspace/compositor-fx/`, git repo + `to_vscode.md` creation still pending
- RageFX icon generation at together.ai: not yet attempted (was queued)
- Config changes (skills fix) may need a git commit in the workspace repo

