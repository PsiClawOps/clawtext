# ClawText Journal — append-only conversation log
# Files: YYYY-MM-DD.jsonl (one per day)
# Never pruned. Git-tracked. Platform-agnostic.
# Recovery: node scripts/restore-context.mjs --channel <id>
*.jsonl linguist-generated=true
